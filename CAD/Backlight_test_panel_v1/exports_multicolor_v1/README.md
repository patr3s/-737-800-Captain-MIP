# CAL-004 multicolor exports

The black face and each white legend STL share the same origin. Import them
as parts of one multicolor object, never auto-arrange the parts independently.

- Variant A uses the 1.0 mm legend export.
- Variant B uses the 0.4 mm legend export.
- The light base is printed once and reused for both optical tests.

See the matching `Ready-to-Print/Backlight-Multicolor-AB-CAL-004` instructions.
